# grodex — run gpt-5.6-sol on your ChatGPT/Codex subscription through the grok CLI.
# Generated by install.sh. Placeholders (__GRODEX_HOME__, __PORT__, __ACCOUNT_ID__)
# are filled in at install time. Safe to edit afterward.

[auth]
# External auth provider: bridges your `codex login` session in as grok's bearer.
auth_provider_command = "__GRODEX_HOME__/codex-auth.sh"
auth_provider_label = "ChatGPT (Codex)"

[models]
default = "gpt-5.6-sol"
default_reasoning_effort = "high"

[model."gpt-5.6-sol"]
model = "gpt-5.6-sol"
name = "GPT-5.6-sol (ChatGPT)"
# Points at the local shim, which forwards to chatgpt.com/backend-api/codex and
# fixes two backend quirks (see codex-proxy.py).
base_url = "http://127.0.0.1:__PORT__"
api_backend = "responses"
# ChatGPT/Codex subscription context cap (the API tier is larger). This only
# controls when grok auto-compacts; the backend enforces the real ceiling.
context_window = 272000
# Opt out of grok's xAI-only `stream_tool_calls` body field (the ChatGPT backend
# rejects unknown params).
stream_tool_calls = false
extra_headers = { "chatgpt-account-id" = "__ACCOUNT_ID__", "OpenAI-Beta" = "responses=experimental", "originator" = "codex_cli_rs", "User-Agent" = "codex_cli_rs/0.144.4" }

[features]
telemetry = false

[cli]
auto_update = false
